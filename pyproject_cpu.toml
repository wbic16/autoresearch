[project]
name = "autoresearch-cpu"
version = "0.1.0"
description = "vTPU autoresearch — CPU/ROCm port"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.9.0",
    "numpy>=2.0.0",
    "pandas>=2.0.0",
    "pyarrow>=14.0.0",
    "requests>=2.32.0",
    "rustbpe>=0.1.0",
    "tiktoken>=0.7.0",
    "torch>=2.5.0",
    "psutil>=5.9.0",
]

# No CUDA-specific index — uses CPU torch from PyPI
